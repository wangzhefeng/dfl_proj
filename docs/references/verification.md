# HTML 调研文档验收记录

日期：2026-09-13（Asia/Shanghai）。目标：`docs/2026-09-13-dfl-predict-and-optimize.html`。

## 实际验证结果

- 已使用独立临时 profile 的 Chrome headless/CDP 打开本地 HTML。常规 browser_exec 因用户 Chrome profile 数据读取失败不可用，未关闭用户浏览器；首次命令行截图产生后进程超时，随后使用受控子进程/CDP 验证成功退出（exit 0）。
- DOM：12 个章节、3 个 SVG 图，重复 ID 0，失效内部锚点 0，外部加载资源 0。
- 桌面 1440×1100 与窄屏 390×844：根页面横向溢出均为 false；宽 SVG 由图容器局部横向滚动承载。
- 导航：滚动至多阶段章节后 activeNav 为 `#rolling`，通过。
- 视觉：桌面首页、窄屏首页和全部 3 张 SVG 均已截图查看，无文字溢出、相互遮挡或箭头穿框。截图位于临时验证目录，不作为项目实验产物。
- 9 个来源链接与 `docs/references/sources.json` 对应，无缺失引用。外部来源已通过正文检索工具读取，本检查不是长期链接可用性保证。
- 教学算例经穷举计算：A 的 MAE=12.75、真实收益=20；B 的 MAE=15、真实收益=70。明确标记为假设示例，而非真实数据或模型收益。
- DFF 训练入口覆盖模型列表的事实已在固定提交 `6f9846f3e2b311b375208e6343f17b3e814b1a64` 的原始文件再次核实。
- Hermes 预览已打开目标 HTML，并读取回标题和完整可见文本。

## 未执行与范围边界

未安装模型依赖、未创建 Python 模型环境、未实现训练模型、未运行任何上游实验、未初始化 Git。打印按钮调用浏览器原生打印；本次未导出并逐页验收 PDF。研究报告不构成模型已复现或生产可用的证明。

## v1.1 场景修订验收（覆盖上文 v1.0 教学算例）

- 主场景已改为次日 96 点负荷预测、15 分钟分辨率、固定峰谷平费率、完整次日功率计划；默认不授权日内重预测或重优化。
- 同步重写训练目标、负荷约束与真实执行补救、三张流程图、96 点教学示例、方法适配与路线；论文的电价预测实验作为原文事实保留，并与本项目区分。
- `node /tmp/dfl-verify.mjs` 实际退出 0：12 个章节、3 个图形、重复 ID 0、失效锚点 0、外部资源 0，桌面与 390px 窄屏均无根页面横向溢出，导航高亮正确。
- 全部 3 张更新后的 SVG 已截图检查，无文字溢出或箭头遮挡。
- 96 点教学例用离散动态规划核验（1 kW 功率网格，对应 0.25 kWh 电量步长）：A 的 MAE=0.0208333 kW、峰时放电=2 kW、节费=0.4 元；B 的 MAE=0.03125 kW、峰时放电=4 kW、节费=0.8 元。实际执行均无反送电且末电量为零。此结果仅验证假设算例，不是现实费率或模型训练结果。
- 用户未给出的电价数值、时段表、反送电政策、需量费及设备参数均保留为待确认项，没有生成正式配置或模型实现。
