# AGENTS.md

## 定位与范围

本项目用于 Decision-Focused Learning（DFL）与端到端 Predict-and-Optimize 的调研和模型实现，重点关注储能套利、用户侧能量管理和滚动优化。当前阶段只有调研文档；不得把论文结果或静态代码审阅称为本项目已复现结果。

## 目录约定

- `docs/`：自包含 HTML 调研、设计与验证记录；图形内嵌，区分文献事实、工程建议和示意数据。
- `docs/references/`：来源索引与版本记录。
- 后续按实际需求创建 `src/dfl_proj/`、`tests/`、`configs/`、`scripts/`、`data/`、`results/`；本阶段不创建空实现或占位训练产物。

## 开发环境

- Python 固定为 3.11.9（`.python-version`），虚拟环境位于项目根目录 `.venv/`，通过 `uv` 管理；运行 `uv sync --locked` 同步环境。
- 当前为非打包研究环境，未安装模型与求解器依赖；新增依赖统一使用 `uv add`，引入模型/求解器前须完成兼容性验证。
- 运行 Python 命令使用 `env -u PYTHONPATH uv run python ...`，避免外部 `PYTHONPATH` 污染；不使用全局安装或其他项目环境执行模型实验。
- 不提交 `.venv/`、密钥、缓存及本地数据／实验结果。

## 科学与工程契约

- 时间连续切分；scaler 仅拟合训练集；预测 origin 的可用信息必须显式定义，不能使用未来实现值。
- 将预测时域、优化时域、执行步长、训练展开长度分别记录；滚动测试保持 SOC 和计费状态连续。
- 区分预测参数进入目标函数与进入约束；约束不确定性必须定义补救、再调度与违例成本。
- 物理参数、硬约束、充放电互斥和终端电量价值不得因可微训练而静默弱化。
- 统一评价真实结算成本、收益、风险、可行性与预测误差；训练代理损失不冒充实际收益。
- 完美信息基线属于事后参考，不得作为可部署策略；比较使用一致的初始/终端条件。
- 功能开发遵循 RED→GREEN→REFACTOR。正式 run 保存配置、环境、数据指纹、预测、动作、状态、真实收益、checkpoint 和日志。
- 不提交、不 push、不删除既有文件，除非明确授权。

## 当前文档验收

HTML 必须离线可打开，无外部脚本、字体或图片依赖；章节和来源锚点有效；桌面与窄屏无正文溢出；图形无文本重叠。研究来源要注明检索日期、论文版本和证据等级。后续实现阶段再补充实际测试命令。
